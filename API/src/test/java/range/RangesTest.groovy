package range

import spock.lang.Specification

/**
 * Test the Ranges.
 */
class RangesTest extends Specification {
    /**
    def "Lookup works correctly"() {
        given:
            Ranges.
            List<Range> testRanges = [
                    Ranges.makeRange(Scheme.Unknown, "4"),
                    Ranges.makeRange(Scheme.Unknown, "41-8"),
            ];

        when:
            Collections.sort(testRanges, Ranges.)

        then:
            testRanges[0].getLength() == 4
    }
     */
}
